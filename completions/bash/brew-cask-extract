# Generated by `brew generate-tap-man-completions`.
_brew_cask_extract() {
  local cur="${COMP_WORDS[COMP_CWORD]}"
  case "${cur}" in
    -*)
      __brewcomp "
      --debug
      --force
      --help
      --no-quarantine
      --no-shard
      --quiet
      --unversioned
      --verbose
      --version
      "
      return
      ;;
    *) ;;
  esac
}
