# Generated by `brew generate-tap-man-completions`.
_brew_generate_tap_man_completions() {
  local cur="${COMP_WORDS[COMP_CWORD]}"
  case "${cur}" in
    -*)
      __brewcomp "
      --debug
      --help
      --no-exit-code
      --no-fork
      --no-pull-requests
      --open-pr
      --quiet
      --tap
      --verbose
      "
      return
      ;;
    *) ;;
  esac
}
