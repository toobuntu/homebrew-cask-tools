# Generated by `brew generate-tap-man-completions`.
# brew cask-extract
_brew_cask_extract() {
  _arguments \
    '--debug[Display any debugging information]' \
    '--force[Overwrite the destination file if it already exists]' \
    '--help[Show this message]' \
    '--no-quarantine[Add a `postflight` block that removes the quarantine xattr]' \
    '--no-shard[Write to a flat `Casks/` directory instead of a sharded one]' \
    '--quiet[Make some output more quiet]' \
    '--unversioned[Copy without adding a version suffix to the cask token]' \
    '--verbose[Make some output more verbose]' \
    '--version[Extract the cask at this specific version from git history]'
}
