# Generated by `brew generate-tap-man-completions`.
# brew man
_brew_man() {
  _arguments \
    '--debug[Display any debugging information]' \
    '(--list)--find[Find all installed formulae that provide the named man page]' \
    '--help[Show this message]' \
    '--html[Open the page as HTML in a browser (requires `--interactive` when used with `--find` or `--list`; respects `HOMEBREW_BROWSER` or `BROWSER`)]' \
    '--interactive[Present a numbered list for interactive selection. Requires `--find` or `--list`]' \
    '(--find)--list[List every man page provided by the named formula]' \
    '--quiet[Make some output more quiet]' \
    '--verbose[Make some output more verbose]' \
    - installed_formula \
    '*:installed_formula:__brew_installed_formulae'
}
